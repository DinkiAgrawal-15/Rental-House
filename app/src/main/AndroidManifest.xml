<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentalhouse">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentalHouse">
        <activity android:name=".tanentform" />
        <activity android:name=".imageslider" />
        <activity android:name=".signup_tanent" />
        <activity android:name=".tanentLogin" />
        <activity android:name=".imageupload" />
        <activity android:name=".houselist" />
        <activity android:name=".propertylist" />
        <activity android:name=".OwnerForm" />
        <activity android:name=".MainActivity" />
        <activity android:name=".signup" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>